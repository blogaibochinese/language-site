1. 修改 styles.css 文件中的 .nav-container 类，将 justify-content 从 center 改为 space-between
2. 移除 .nav-brand 类的绝对定位和 left 属性，使其自然排列在左侧
3. 确保 .nav-menu 类的样式保持不变，使其在右侧对齐
4. 检查响应式设计，确保在移动设备上显示正常

这样修改后，logo会在左侧，菜单项会在右侧，两者之间有足够的空间，避免重叠。