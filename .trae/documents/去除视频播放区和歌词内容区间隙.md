1. 定位问题：`.main-content` 类的 `padding-top` 设置为 `90vh`，导致歌词内容区域从视口高度90%处开始，与视频播放区产生大间隙
2. 解决方案：将 `.main-content` 的 `padding-top` 调整为合理值，如 `100px`
3. 具体修改：修改第147行的 CSS 样式，将 `padding: 90vh 20px 100px;` 改为 `padding: 100px 20px 100px;`
4. 预期效果：视频播放区和歌词内容区之间的间隙消失，页面布局更加紧凑合理

