### 问题分析
1. **现象**：groups.html页面的移动端底部按钮没有固定显示，只有拉到底部才会显示
2. **原因**：groups.html使用了自定义的Tailwind-like工具类实现固定定位，但可能存在样式优先级问题或z-index值不够高
3. **参考**：Hanzi.html的移动端底部导航栏实现了正确的固定定位效果

### 修复方案
1. **采用Hanzi.html的mobile-nav类结构**：将groups.html中的移动端底部导航栏改为使用`mobile-nav`类
2. **添加完整的CSS样式**：确保`mobile-nav`类在移动端下具有正确的固定定位属性
3. **设置合适的z-index**：确保底部导航栏显示在其他内容之上
4. **保持样式一致性**：确保修复后的底部导航栏与Hanzi.html保持一致的外观和交互效果

### 具体修改步骤
1. **修改groups.html的HTML结构**：将第850-873行的移动端底部导航栏代码替换为Hanzi.html中的`mobile-nav`结构
2. **添加mobile-nav类的CSS样式**：在groups.html的`<style>`标签中添加完整的`mobile-nav`类样式，包括响应式设计
3. **移除冲突的样式**：确保没有其他样式会覆盖`mobile-nav`的固定定位属性
4. **测试修复效果**：在移动端设备上测试底部导航栏是否正确固定显示

### 预期效果
- 移动端访问groups.html时，底部导航栏会固定显示在屏幕底部
- 底部导航栏的外观和交互效果与Hanzi.html保持一致
- 不会影响页面其他内容的正常显示